@model IEnumerable<Competitions.Domain.Dtos.Extracurriculars.GetExtracurricularDetailsDto>


@{
    Layout = "_Layout";
}
<div class="d-flex align-items-start align-items-sm-center justify-content-between mb-2 flex-column flex-sm-row">
    <h3>لیست فوق برنامه ها</h3>
</div>


<form method="get">

    <div class="table-overflow card shadow-sm">
        <div class="table ">
            <table class="table text-center" width="100%">
                <thead>
                    <tr>
                        <th class="table__row">ردیف</th>
                        <th>نام دوره</th>
                        <th>رشته ورزشی</th>
                        <th>نوع مخاطبین</th>
                        <th>مکان برگزاری</th>
                        <th>مشاهده</th>
                    </tr>
                </thead>
                <tbody>
                    @for ( int i = 0 ; i < Model.Count() ; i++ )
                    {
                        <tr>
                            <td>
                                @(i + 1)
                            </td>
                            <td>@Model.ElementAt(i).Name</td>
                            <td>@Model.ElementAt(i).Sport</td>
                            <td>@Model.ElementAt(i).AudienceType</td>
                            <td>@Model.ElementAt(i).Place</td>
                            <td>
                                <div class="table__action">
                                    <a class="btn btn--show" asp-action="Details" asp-route-id="@Model.ElementAt(i).Id"><i class="fa fa-eye"></i></a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</form>